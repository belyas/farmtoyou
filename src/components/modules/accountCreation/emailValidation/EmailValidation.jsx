//on the frontend, this component returns an email validation message to the user pre validation.
//if validation is successful, it returns success message
//if not successful, it returns error message

export default function EmailValidation(){
    return (
        <>
        <p>
        from email validation component
        </p>
        </>
    )
}